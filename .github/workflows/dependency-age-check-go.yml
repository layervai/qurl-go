# Dependency Age Check (Go modules)
# Thin shim calling shared reusable workflow.
# Source: https://github.com/layervai/ops-routines-workflows/blob/main/.github/workflows/age-check-go.yml
name: Dependency Age Check (Go)

# Fires on every PR (no path filter) so the status check always reports.
# This lets branch protection require it without the path-filtered-
# required-check footgun (a required check that never fires blocks
# merge with "Expected — Waiting for status to be reported").
# The reusable's script exits 0 quickly when no go.sum changes are
# in the diff — see "No go.sum changes detected" in age-check-go.sh.
on:
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same PR branch — the reusable workflow
# makes sequential registry calls; no reason to let two race for an
# outdated diff.
concurrency:
  group: dep-age-go-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  age-check:
    # No outer `if:` for `age-check-bypass`. The required status check
    # is `age-check / check-go-age` (caller-job / reusable-inner-job).
    # Skipping here means the reusable never runs and the inner job
    # never reports → required check stuck at "Expected — Waiting"
    # forever. The reusable's inner `if:` handles bypass and reports
    # SKIPPED, which satisfies the required check.
    # Note: `timeout-minutes` is intentionally NOT set here. Per GH
    # docs (https://docs.github.com/en/actions/using-workflows/reusing-workflows#supported-keywords-for-jobs-that-call-a-reusable-workflow)
    # only if/name/needs/permissions/secrets/strategy/uses/with/concurrency
    # are valid on a job that calls a reusable workflow. The reusable
    # workflow itself sets `timeout-minutes: 5` internally.
    uses: layervai/ops-routines-workflows/.github/workflows/age-check-go.yml@e8c55b3945b963d0b6a87095214693731c024560 # v0.11.0
    with:
      min_age_days: 7
      # First-party modules cut from this org's own protected default
      # branches. The reusable skips ONLY these modules' age delay, and only
      # after the Go proxy origin, exact GitHub tag, verified target commit
      # and default-branch ancestry all agree; anything unverifiable fails
      # closed. This is far narrower than `age-check-bypass`, which skips the
      # entire check for every dependency in the PR.
      # Matching rule (`module_is_exempt` in the reusable's age-check-go.sh): an
      # entry WITHOUT a trailing slash matches ONE module path exactly. So the
      # entries below do not cover `qurl-go-extra`, `qurl-golang`, or a future
      # `qurl-go/v2` major path — a v2 bump falls back to the cooldown until
      # its own line is added. Only an entry ending in `/` prefix-matches, and
      # then only at a path-segment boundary. Keep these exact; never shorten
      # one to a bare `github.com/layervai/`.
      # qurl-go itself is listed for the nested awsstore module, which requires
      # the root module at a published tag with no local `replace`.
      exempt_module_prefixes: |
        github.com/layervai/qurl-conformance
        github.com/layervai/qurl-go
      # Post a sticky PR comment when an age-check fails, listing each
      # pin with its eligible-after date and days remaining. The PR
      # author / reviewer sees the retry-after date inline on the PR
      # instead of having to click into the run log to find it. Sticky
      # comment is updated in place on re-runs and deleted on a clean
      # pass; soft-fails to a `::notice` if the token can't write.
      comment_on_failure: true
    permissions:
      contents: read
      # Required for `comment_on_failure: true` to actually post.
      # Dependabot PRs default to read-only and need this grant
      # explicitly. Without it, the workflow still fails the age check
      # correctly — only the inline comment is suppressed.
      pull-requests: write
